# Simple LS

A simple directory listing tool, similar to the `ls` command.

## Build

```bash
make
```

## Run

```bash
./app [directory]
```

If no directory is specified, the current directory is used.

Example:

```bash
./app /tmp
```